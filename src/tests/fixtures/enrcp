#/bin/bash
for arg in "$@"; do
    #echo $arg
    if [ "$arg" = "exit_0" ]; then
        echo "$0 $@ ---> exiting status  0"
        exit 0
    fi
    if [ "$arg" = "exit_1" ]; then
        echo "$0 $@ ---> exiting status  1"
        exit 1
    fi
done
