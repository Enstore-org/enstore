FILE = TABLE
PRODUCT=encp

FLAVOR = ANY
QUALIFIERS = ""
    ACTION=SETUP
	setupEnv()
	proddir()
	pathPrepend(PATH, ${UPS_PROD_DIR})
	envSet(ENSTORE_CONFIG_PORT, 7510)
	envSet(ENSTORE_CONFIG_HOST, pcfarm9.fnal.gov)
	envSet(TRACE_KEY, /tmp/etc/trace.`hostname`)
