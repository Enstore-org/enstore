Usually, you never have to do this, but ...

When enstoredb is in trouble, call developer first.

In an extreme emergency when no developer is available and no other
known method can revive the database ...

Assuming there is a good file system on *srv0 and no database server is
running, run the following script as root on srv0:

$ENSTORE_DIR/databases/scripts/restore_enstoredb
