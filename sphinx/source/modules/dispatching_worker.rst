dispatching_worker
==================

.. inheritance-diagram:: dispatching_worker

.. include:: ../toc.txt

.. automodule:: dispatching_worker
   :synopsis: Dispatching Worker is a core of all enstore servers. It receives client UDP requests, convert them into dictionary (enstore ticket) and execute metod, specified by ticket['work'] in a loop.

