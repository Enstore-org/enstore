Do the following to install the Enstore cgi scripts on the node which
runs the web server -

	o set up enstore

	o Find the supported directory for cgi scripts.  Ususally called
		cgi-bin.

	o run the command 'install-web-files'

