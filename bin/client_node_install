#!/bin/sh
set -x

lockdir=/var/lock/hsm
lockfile=/var/lock/hsm/lockfile

if [ ! -d $lockdir ] ; then  mkdir -p $lockdir ; fi


touch $lockfile

# Should investigate it the sticky bit could protect us from a malicios user
# deleting this file. 

chmod +w $lockfile



