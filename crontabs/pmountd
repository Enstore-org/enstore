#This cronjob watched for pmountd on *srv1n and restarts it if it is missing
# that can mount pnfs.
0-59/5 * * * * root source /usr/local/etc/setups.sh; setup enstore; $ENSTORE_DIR/sbin/ecron -p pmountd  $ENSTORE_DIR/sbin/pmountd_nanny.sh >/dev/null 2>/dev/null
